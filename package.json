{
  "scripts": {
    "prebuild": "rm -rf dist",
    "build": "parcel build --no-cache --no-source-maps src/index.html"
  },
  "devDependencies": {
    "parcel": "^2.12.0",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "@dfinity/agent": "^1.3.0",
    "@dfinity/auth-client": "^1.3.0",
    "@dfinity/identity": "^1.3.0",
    "@dfinity/principal": "^1.3.0"
  }
}
